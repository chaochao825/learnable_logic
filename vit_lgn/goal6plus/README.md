# Goal6plus ViT-LGN Snapshot

Source: `/home/spco/sow_linear/ViT-LGN_goal6plus` on the 210 server.

This snapshot keeps the source files needed to inspect and rerun the current
ViT-LGN + LogicFFN experiments and the CLS-only top-k token mixer ablation.
Large `runs/`, `logs/`, `data/`, compiled extensions, and caches are excluded.
Compact configs and metrics are under `artifacts/`.
